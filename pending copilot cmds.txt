Copilot, add a full-screen video background to my homepage.

1. Create a new component:
/modules/Background/VideoBackground.tsx

The component should:
- Use <video> as a background element
- Include:
    <video
      src="/videos/home-bg.mp4"
      autoPlay
      muted
      loop
      playsInline
      className="video-bg"
    />
- Wrap the video in a div with className="video-bg-container"
- Export the component

2. Add styling in /styles/themes/cyberpunk.scss:
- .video-bg-container:
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    z-index: -2;

- .video-bg:
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.55); /* darken slightly so text/UI is readable */

3. In /app/page.tsx (homepage):
- Import VideoBackground:
    import VideoBackground from "@/modules/Background/VideoBackground";

- Render <VideoBackground /> at the top of the page layout, BEFORE the main content.

Example:
return (
  <>
    <VideoBackground />
    <Hero />
    <IdentitySection />
    <StatusStrip />
  </>
);

4. Ensure the fixed background does not interfere with scrolling:
- Body and main content should have position: relative; and z-index: 1

Add a semi-transparent overlay layer above the video background:
- Create a div.video-overlay
- Apply background: rgba(0,0,0,0.50)
- Position it fixed, full-screen, z-index: -1


Copilot, build a full cinematic intro boot sequence.

1. In /modules/Intro/IntroSequence.tsx:
   - Create a fullscreen overlay that plays on page load.
   - Show a sequence of typewriter-style terminal lines, one after another:
        "> Initializing DukeOS..."
        "> Loading neural modules..."
        "> Connecting to NeonGrid Network..."
        "> Decrypting identity..."
        "> Boot sequence complete."
   - After the text finishes, fade out the intro overlay smoothly.
   - Use a state variable "introDone" to hide the sequence once played.

2. Add localStorage support:
   - Store "duke_intro_played": "true" after first play.
   - If it exists, skip the intro entirely.
   - Also add a function triggerReboot() that removes the key and replays the intro.

3. Add required SCSS in /styles/features/intro.scss:
   - Fullscreen black background.
   - Green terminal text (#00ff9d).
   - Typewriter caret blinking animation.
   - Glitch flicker animation for title.
   - Smooth opacity fade transitions.

4. Import this component in /app/page.tsx:
   <IntroSequence />

5. Ensure the intro sits ABOVE all content:
   - Intro wrapper uses z-index: 9999; position: fixed; inset: 0;


Copilot, build a custom hologram cursor.

1. Create /modules/Cursor/CustomCursor.tsx:
   - Track mouse position using an event listener.
   - Render a <div> that follows the cursor with transform: translate().
   - Cursor appearance:
       - Neon cyan ring (border-radius: 50%)
       - Slight outer glow
       - Smooth easing (transition: transform 70ms)

2. Add hover effect for clickable elements:
   - When hovering any <a>, <button>, <.clickable>:
       Increase cursor size slightly and add a “lock-on” ring.

3. Add SCSS in /styles/features/cursor.scss:
   - .cursor-core: base dot/ring
   - .cursor-lock: secondary ring on hover
   - Neon glow using box-shadow: 0 0 8px var(--accent)

4. Hide the default browser cursor globally:
   * {
     cursor: none !important;
   }

5. Import CustomCursor in /app/layout.tsx so it appears on every page.


Copilot, build a sliding Matrix-style console panel.

1. Create /modules/Console/MatrixConsole.tsx:
   - A hidden panel that slides from top with position: fixed.
   - Opens when user presses "~".
   - Closes when user presses "Escape".
   - Use state variable "open" to track visibility.

2. Inside the console:
   - A text input field mimicking a terminal.
   - Green matrix-style text.
   - Commands to support:
         help   → lists commands
         stats  → prints info from siteData.profile
         theme  → switches theme (placeholder)
         clear  → clears console output
         reboot → triggers IntroSequence replay

3. Add SCSS in /styles/features/console.scss:
   - Dark background (#020)
   - Neon green text (#00ff70)
   - Slight scanline effect
   - Sliding animation: transform: translateY(-100%) → translateY(0)

4. Import MatrixConsole in /app/layout.tsx:
   <MatrixConsole />

5. Ensure console sits above main content when open (z-index: 9000)


Copilot, update the MatrixConsole component to support sound effects.

1. Inside /modules/Console/MatrixConsole.tsx:
   - Import nothing; use new Audio() for sound.
   - Add two helper functions:
        playAccess()
        playDenied()
     each calling new Audio("/sfx/terminal-access.mp3") and new Audio("/sfx/terminal-denied.mp3").

2. Whenever a user enters a VALID command:
       playAccess();
   Valid commands include:
       help, stats, clear, reboot, theme

3. Whenever the user enters an INVALID command:
       playDenied();

4. Ensure the audio plays client-side only:
   - Wrap audio calls in a check:
        if (typeof window !== "undefined") { ... }

5. Add a small cooldown (100–200ms) so spamming Enter doesn’t overlap audio too much.

6. Keep sound subtle (set volume = 0.25 by default).


Copilot, build a full chaos-mode "System Meltdown" event for DukeOS.

=== 1. Create a new component ===
Create the file:
/modules/Chaos/Meltdown.tsx

This component must:
- Render a fullscreen overlay above EVERYTHING (z-index: 999999)
- Remain hidden until triggered
- Handle a 10-second meltdown sequence
- Auto-reset the entire UI when finished

State:
- meltdownActive: boolean
- phase: number (0 to 6)
- cooldown: boolean

Triggers:
- meltdown must activate ONLY when a function triggerMeltdown() is called
- meltdown cannot activate again for 30 seconds (cooldown)

Export:
export { Meltdown, triggerMeltdown }

=== 2. Meltdown Sequence Timing (MAX 10 SECONDS) ===
Use setTimeout to run these phases in order:

PHASE 0 (0.0s–0.4s):
- Freeze UI (body pointer-events: none)
- Freeze cursor movement
- Darken entire screen slightly
- Play "/sfx/alarm_crisis.mp3" once
- Begin initial flicker animation

PHASE 1 (0.4s–1.5s):
- Show big red flashing overlay:
    "!!! SYSTEM BREACH DETECTED !!!"
    "!!! CRITICAL FAILURE !!!"
- Rapid red/white strobe (CSS animation)
- Small glitch bars flicker

PHASE 2 (1.5s–2.2s):
- Apply neon “shatter” effect:
   * colored fragment offsets
   * quick pixel inversion burst
- Cursor becomes large red hazard icon
- Alarm plays again (shortened)

PHASE 3 (2.2s–4.5s) — CHAOS STORM:
- Heavy glitch layers:
   Random terminal lines:
     "> FIREWALL LOST"
     "> DATA NODES UNSTABLE"
     "> MEMORY CORRUPTED"
     "> NEURAL LINK FAILURE"
- Video background distortion:
   invert, hue-rotate, blur, shake
- Entire screen shakes lightly
- Cursor jitter animation
- Borders + neon edges pulse rapidly

PHASE 4 (4.5s–5.7s):
- Sudden BRIGHT flash → HARD blackout
- Silence for 0.3s
- Screen fully black

PHASE 5 (5.7s–7.0s) — RECOVERY:
- Show terminal text:
    "> DUKEOS RECOVERY MODE ENGAGED"
    "> Restoring system files..."
    "> Cleaning corrupted nodes..."
- Gentle hum audio (optional)

PHASE 6 (7.0s–10.0s):
- Fade UI back into normal state
- Cursor resets to normal
- Console prints:
    "> SYSTEM STATUS: STABLE"
    "> OPERATOR: DUKE"
- End meltdown

After 10 seconds:
- cleanup meltdown overlay
- restore UI pointer-events
- set cooldown to TRUE for 30 seconds

=== 3. SCSS Styling (Create file) ===
Add this file:
/styles/features/meltdown.scss

Include styles:
- .meltdown-overlay (fixed, fullscreen)
- .meltdown-flash (red flashing)
- .meltdown-text (huge hazard text)
- .meltdown-glitch (glitch bar elements)
- .meltdown-shatter (fragment transforms)
- .meltdown-distort (filters applied to main UI)
- .meltdown-blackout (pure black screen)
- cursor-danger mode red glowing reticle
- screen shake animation via @keyframes
- bright-to-dark flash animation

Use:
position: fixed;
inset: 0;
pointer-events: none;
mix-blend-mode: screen or overlay for effects.

=== 4. Integrate Meltdown ===
In /app/layout.tsx:
- Import and render <Meltdown />

Global export:
- Add triggerMeltdown() to window:
   if (typeof window !== "undefined") {
      window.triggerMeltdown = triggerMeltdown;
   }

=== 5. Console Integration ===
Open /modules/Console/MatrixConsole.tsx:

In command handler:
If user enters:
- "hack"
- "break"
- "selfdestruct"
- "duke.exe overload"

Then:
- Check cooldown:
    if (cooldown) playDenied() and print:
      "> meltdown cooling down. try again later."
- Else:
    playAccess()
    call triggerMeltdown()
    print:
      "> WARNING: meltdown initiated..."

=== 6. Cursor Panic Mode ===
In CustomCursor component:
- Add a prop or global flag for meltdownActive
- When meltdownActive = true:
    - cursor turns into red hazard reticle
    - jitter animation applied
    - glow colors shift to #ff0033

When meltdown ends:
    revert cursor to normal

=== 7. Video Distortion (applied globally) ===
When meltdownActive:
- Add class "meltdown-distort" to <body>

meltdown-distort CSS:
filter: hue-rotate(120deg) brightness(1.4) contrast(1.8) blur(2px);
animation: shake 0.1s infinite;

When meltdown ends:
- remove class

=== 8. Cooldown Support ===
After meltdown finishes:
- cooldown = true
- After 30 seconds:
    cooldown = false

---

Make sure the entire meltdown sequence never exceeds 10 seconds total.
All chaos should be contained within that window.


Copilot, modify the meltdown system so that:

- /sfx/alarm_crisis.mp3 loops continuously during PHASE 1 to PHASE 4.
- The alarm must stop IMMEDIATELY when PHASE 4 ends and the screen blacks out.
- Use a single Audio instance, not multiple overlapping ones.
- alarmAudio.loop = true during chaos.
- At PHASE 5 (recovery), do NOT restart the alarm.
- Optionally play a soft recovery hum after blackout.

Make sure stopping the alarm interrupts it cleanly without fade.
